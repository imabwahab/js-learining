// Implement a function that removes duplicates from an array using Set.

let array = [1, 2, 2, 4,  6, 6];

let set = new Set(array);

set.forEach(v => console.log(v));

// A set stores only the unique values.

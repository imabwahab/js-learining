// Show the difference between == and === with examples.

console.log('== operator only compares the values, if both values are equal then it returns true.');

console.log('=== operator compares both values and data types of two variables.');

let x = 1;
let y = 2;
let z = '22'

console.log(x==y);
console.log(y===x);
console.log(z === 22);
console.log(z==22);